kind: Deployment
apiVersion: apps/v1
metadata:
  name: golang-sample-main
  labels:
    app: golang-sample-main
    app.kubernetes.io/component: golang-sample-main
    app.kubernetes.io/instance: golang-sample-main
    app.kubernetes.io/name: golang-sample-main
    app.kubernetes.io/part-of: golang-sample
spec:
  replicas: 1
  selector:
    matchLabels:
      app: golang-sample-main
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: golang-sample-main
        deploymentconfig: golang-sample-main
    spec:
      containers:
        - name: golang-sample-main
          image: image-registry.openshift-image-registry.svc:5000/a-test/golang-sample-main
          resources: {}
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: Always
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
      schedulerName: default-scheduler
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600
